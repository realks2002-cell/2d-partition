@import "tailwindcss";

/* =========================================================
   칸막이Go — Refined Atelier theme (commercial grade)
   ========================================================= */

:root {
  /* Base surfaces */
  --bg: #fafaf7;
  --surface: #ffffff;
  --surface-2: #f4f3ee;
  --surface-3: #ebe9e2;

  /* Ink */
  --ink: #0e0e0d;
  --ink-2: #2a2926;
  --ink-3: #4e4c46;
  --muted: #8a8880;
  --muted-2: #b8b5ac;

  /* Lines */
  --line: #e8e6dd;
  --line-2: #dcd9cf;

  /* Brand accent — deep terracotta */
  --accent: #9b2f1a;
  --accent-hover: #7f2614;
  --accent-tint: rgba(155, 47, 26, 0.08);

  /* Semantic */
  --success: #2f6a4f;
  --danger: #a8352a;

  /* Shape */
  --radius-xs: 4px;
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-xl: 28px;

  /* Shadows — subtle, commercial */
  --shadow-1: 0 1px 2px rgba(14, 14, 13, 0.04), 0 1px 1px rgba(14, 14, 13, 0.03);
  --shadow-2: 0 4px 12px rgba(14, 14, 13, 0.06), 0 2px 4px rgba(14, 14, 13, 0.04);
  --shadow-3: 0 12px 28px -8px rgba(14, 14, 13, 0.12), 0 4px 8px rgba(14, 14, 13, 0.05);
  --shadow-cta: 0 1px 0 rgba(255, 255, 255, 0.08) inset,
    0 8px 24px -10px rgba(14, 14, 13, 0.45);

  /* Motion */
  --ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --ease-in: cubic-bezier(0.55, 0.06, 0.68, 0.19);

  /* Semantic */
  --background: var(--bg);
  --foreground: var(--ink);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-ink: var(--ink);
  --color-muted: var(--muted);
  --color-accent: var(--accent);
  --color-line: var(--line);
  --font-display: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
  --font-sans: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
  --font-mono: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
}

* {
  -webkit-tap-highlight-color: transparent;
}

html,
body {
  -webkit-text-size-adjust: 100%;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  background: var(--bg);
  color: var(--ink);
  font-family: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
  font-size: 15px;
  line-height: 1.55;
  font-feature-settings: "ss01", "cv11", "kern";
  min-height: 100vh;
  min-height: 100dvh;
}

/* Safe areas */
.pt-safe {
  padding-top: env(safe-area-inset-top);
}
.pb-safe {
  padding-bottom: env(safe-area-inset-bottom);
}

/* ======== Typography ======== */

.display {
  font-family: var(--font-display);
  letter-spacing: -0.025em;
  line-height: 1.1;
  font-weight: 700;
  color: var(--ink);
}

.display-tight {
  font-family: var(--font-display);
  letter-spacing: -0.03em;
  line-height: 1.1;
  font-weight: 700;
}

.serif {
  font-family: var(--font-display);
  font-weight: 600;
}

.mono,
.numeric {
  font-family: var(--font-mono);
  font-feature-settings: "tnum";
  font-variant-numeric: tabular-nums;
}

.eyebrow {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--muted);
}

.label {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  letter-spacing: 0.01em;
  color: var(--ink-3);
}

.caption {
  font-size: 12.5px;
  line-height: 1.55;
  color: var(--muted);
}

/* ======== Buttons ======== */

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  font-family: var(--font-sans);
  font-weight: 500;
  font-size: 15px;
  letter-spacing: -0.005em;
  border-radius: var(--radius-md);
  transition:
    transform 180ms var(--ease),
    background-color 180ms var(--ease),
    box-shadow 180ms var(--ease),
    border-color 180ms var(--ease);
  cursor: pointer;
  user-select: none;
}

.btn-primary {
  min-height: 48px;
  padding: 0 20px;
  background: var(--ink);
  color: var(--surface);
  border: 1px solid var(--ink);
  box-shadow: var(--shadow-cta);
}
.btn-primary:active:not(:disabled) {
  transform: translateY(1px);
  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.08) inset,
    0 4px 12px -6px rgba(14, 14, 13, 0.3);
}
.btn-primary:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  box-shadow: none;
}

.btn-secondary {
  min-height: 48px;
  padding: 0 18px;
  background: var(--surface);
  color: var(--ink);
  border: 1px solid var(--line-2);
  box-shadow: var(--shadow-1);
}
.btn-secondary:active:not(:disabled) {
  background: var(--surface-2);
  transform: translateY(1px);
}
.btn-secondary:disabled {
  opacity: 0.4;
}

.btn-ghost {
  min-height: 44px;
  padding: 0 14px;
  background: transparent;
  color: var(--ink);
  border: 1px solid transparent;
  font-size: 14px;
}
.btn-ghost:active:not(:disabled) {
  background: var(--surface-2);
}

.btn-icon {
  width: 44px;
  height: 44px;
  padding: 0;
  background: var(--surface);
  color: var(--ink);
  border: 1px solid var(--line-2);
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-1);
  transition:
    background 180ms var(--ease),
    transform 180ms var(--ease);
}
.btn-icon:active {
  background: var(--surface-2);
  transform: scale(0.96);
}

/* ======== Cards & surfaces ======== */

.surface {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius-md);
}

.surface-raised {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-2);
}

.divider {
  height: 1px;
  background: var(--line);
  border: 0;
}

/* ======== Chips ======== */

.chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: var(--ink);
  color: var(--surface);
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.04em;
  border-radius: 100px;
}

.chip-soft {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: var(--surface);
  color: var(--ink-3);
  border: 1px solid var(--line-2);
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.04em;
  border-radius: 100px;
}

.chip-accent {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: var(--accent-tint);
  color: var(--accent);
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.04em;
  border-radius: 100px;
}

/* ======== Form primitives ======== */

.field {
  display: block;
}

.field-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--ink-3);
  margin-bottom: 8px;
}
.field-label .unit {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--muted);
  font-weight: 400;
}

.num-input {
  width: 100%;
  background: var(--surface);
  border: 1px solid var(--line-2);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  font-family: var(--font-mono);
  font-size: 15px;
  font-feature-settings: "tnum", "zero";
  color: var(--ink);
  min-height: 42px;
  transition:
    border-color 180ms var(--ease),
    box-shadow 180ms var(--ease);
}
.num-input:focus {
  outline: none;
  border-color: var(--ink);
  box-shadow: 0 0 0 3px rgba(14, 14, 13, 0.08);
}

.text-input {
  width: 100%;
  background: var(--surface);
  border: 1px solid var(--line-2);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  font-family: var(--font-sans);
  font-size: 14px;
  color: var(--ink);
  min-height: 42px;
  transition:
    border-color 180ms var(--ease),
    box-shadow 180ms var(--ease);
}
.text-input::placeholder {
  color: var(--muted-2);
}
.text-input:focus {
  outline: none;
  border-color: var(--ink);
  box-shadow: 0 0 0 3px rgba(14, 14, 13, 0.08);
}

/* ======== Segmented control ======== */

.seg {
  display: grid;
  background: var(--surface-2);
  padding: 4px;
  border-radius: var(--radius-md);
  gap: 2px;
}
.seg > button {
  min-height: 40px;
  font-family: var(--font-sans);
  font-weight: 500;
  font-size: 13px;
  color: var(--ink-3);
  background: transparent;
  border-radius: calc(var(--radius-md) - 4px);
  transition:
    background 220ms var(--ease),
    color 220ms var(--ease),
    box-shadow 220ms var(--ease);
}
.seg > button[data-active="true"] {
  background: var(--surface);
  color: var(--ink);
  box-shadow: var(--shadow-1);
  font-weight: 600;
}

/* ======== Counter ======== */

.counter {
  display: inline-flex;
  align-items: center;
  gap: 0;
  background: var(--surface-2);
  border-radius: var(--radius-md);
  padding: 4px;
}
.counter > button {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--surface);
  color: var(--ink);
  border-radius: calc(var(--radius-md) - 4px);
  box-shadow: var(--shadow-1);
  transition: transform 180ms var(--ease);
}
.counter > button:active {
  transform: scale(0.92);
}
.counter > .value {
  min-width: 56px;
  text-align: center;
  font-family: var(--font-mono);
  font-size: 18px;
  font-weight: 500;
  font-feature-settings: "tnum";
  color: var(--ink);
}
.counter > .counter-input {
  width: 56px;
  text-align: center;
  font-family: var(--font-mono);
  font-size: 18px;
  font-weight: 500;
  font-feature-settings: "tnum";
  color: var(--ink);
  background: transparent;
  border: 0;
  outline: 0;
  padding: 0;
  -moz-appearance: textfield;
}
.counter > .counter-input::-webkit-outer-spin-button,
.counter > .counter-input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  margin: 0;
}
.counter > .counter-input:focus {
  background: var(--surface-2);
  border-radius: 4px;
}

/* ======== Swatch ======== */

.swatch {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 10px 6px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius-md);
  transition:
    border-color 200ms var(--ease),
    box-shadow 200ms var(--ease);
}
.swatch[data-active="true"] {
  border-color: var(--ink);
  box-shadow: 0 0 0 1px var(--ink), var(--shadow-1);
}
.swatch .dot {
  width: 26px;
  height: 26px;
  border-radius: 50%;
  border: 1px solid rgba(14, 14, 13, 0.1);
  box-shadow: inset 0 0 0 2px var(--surface),
    inset 0 -2px 6px rgba(0, 0, 0, 0.1);
}
.swatch .name {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--ink-3);
}

/* ======== Pill numeric ======== */

.pill-num {
  min-width: 42px;
  height: 42px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-mono);
  font-size: 14px;
  font-weight: 500;
  background: var(--surface);
  border: 1px solid var(--line-2);
  border-radius: var(--radius-md);
  color: var(--ink);
  font-feature-settings: "tnum";
  transition:
    background 180ms var(--ease),
    border-color 180ms var(--ease),
    transform 180ms var(--ease);
}
.pill-num:active {
  transform: scale(0.96);
}
.pill-num[data-active="true"] {
  background: var(--ink);
  color: var(--surface);
  border-color: var(--ink);
}

/* ======== Action bar (bottom fixed) ======== */

.action-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 12px 20px calc(12px + env(safe-area-inset-bottom));
  background: var(--bg);
  border-top: 1px solid var(--line);
  z-index: 50;
}
.action-bar > .inner {
  max-width: 480px;
  margin: 0 auto;
}

/* ======== Brand mark ======== */

.wordmark {
  font-family: var(--font-display);
  font-variation-settings: "opsz" 14;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--ink);
}

/* ======== Section header pattern ======== */

.section-head {
  display: flex;
  align-items: baseline;
  gap: 8px;
  margin-bottom: 8px;
}
.section-head .idx {
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.06em;
  color: var(--muted);
  min-width: 22px;
}
.section-head .title {
  font-family: var(--font-sans);
  font-size: 16px;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--ink);
}
.section-head .tag {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--muted);
  text-transform: lowercase;
}
.section-head .rule {
  flex: 1;
  height: 1px;
  background: var(--line);
}

/* ======== Animations ======== */

@keyframes riseFade {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.rise {
  animation: riseFade 520ms var(--ease) both;
}
.rise-1 {
  animation-delay: 60ms;
}
.rise-2 {
  animation-delay: 140ms;
}
.rise-3 {
  animation-delay: 220ms;
}
.rise-4 {
  animation-delay: 300ms;
}

@keyframes pulse-dot {
  0%,
  100% {
    opacity: 1;
  }
  50% {
    opacity: 0.4;
  }
}
.dot-pulse {
  animation: pulse-dot 1.6s ease-in-out infinite;
}

/* ======== Focus visible ======== */

button:focus-visible,
input:focus-visible,
select:focus-visible,
a:focus-visible {
  outline: 2px solid var(--ink);
  outline-offset: 2px;
}

/* ======== Spec page compact overrides ========
   - Buttons: 30% smaller (70% of original)
   - Inputs: 20% smaller (80% of original)
*/

.spec-compact .btn {
  min-height: 34px;
  padding: 0 14px;
  font-size: 13px;
  gap: 7px;
  border-radius: 10px;
}
.spec-compact .btn-primary {
  min-height: 34px;
}
.spec-compact .btn-secondary {
  min-height: 34px;
}
.spec-compact .btn-icon {
  width: 31px;
  height: 31px;
}
.spec-compact .btn-icon svg {
  width: 14px;
  height: 14px;
}

.spec-compact .seg {
  padding: 3px;
  border-radius: 10px;
}
.spec-compact .seg > button {
  min-height: 28px;
  font-size: 11px;
  border-radius: 7px;
}

.spec-compact .counter {
  padding: 3px;
  border-radius: 10px;
}
.spec-compact .counter > button {
  width: 28px;
  height: 28px;
  border-radius: 7px;
}
.spec-compact .counter > button svg {
  width: 13px;
  height: 13px;
}
.spec-compact .counter > .value {
  min-width: 40px;
  font-size: 14px;
}
.spec-compact .counter > .counter-input {
  width: 40px;
  font-size: 14px;
}

.spec-compact .pill-num {
  min-width: 30px;
  height: 30px;
  padding: 0 7px;
  font-size: 11px;
  border-radius: 10px;
}

.spec-compact .swatch {
  padding: 7px 4px;
  border-radius: 10px;
}
.spec-compact .swatch .dot {
  width: 18px;
  height: 18px;
}
.spec-compact .swatch .name {
  font-size: 10px;
}

.spec-compact .chip-soft {
  padding: 4px 9px;
  font-size: 10px;
}

/* Inputs 20% smaller */
.spec-compact .num-input {
  min-height: 34px;
  padding: 8px 10px;
  font-size: 12px;
  border-radius: 6px;
}
.spec-compact .text-input {
  min-height: 34px;
  padding: 8px 10px;
  font-size: 11px;
  border-radius: 6px;
}
.spec-compact .field-label {
  font-size: 11px;
  margin-bottom: 6px;
}
.spec-compact .field-label .unit {
  font-size: 9px;
}
.spec-compact .section-head .title {
  font-size: 13px;
}
.spec-compact .section-head .idx {
  font-size: 10px;
}
.spec-compact .section-head .tag {
  font-size: 10px;
}

/* Action bar (fixed bottom) primary button — keep compact too */
.spec-compact .action-bar .btn-primary {
  min-height: 40px;
  font-size: 13px;
}
